import { StatusBar } from "expo-status-bar";
import { SafeAreaView, StyleSheet, Text, View } from "react-native";

export default function App() {
  return (
    <SafeAreaView>
      <Text className="text-red-500">Open up App.tsx to start working on yo app!</Text>
      <StatusBar />
    </SafeAreaView>
  );
}
